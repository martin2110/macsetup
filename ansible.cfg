[defaults]
# Configuration for macOS setup playbook
inventory = inventory.yml
host_key_checking = False
retry_files_enabled = False
gathering = smart
fact_caching = memory

# Output settings
stdout_callback = yaml
bin_ansible_callbacks = True

# Role paths
roles_path = roles

# SSH settings
timeout = 10