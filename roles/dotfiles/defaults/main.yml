---
# Default variables for dotfiles role

# Git configuration
git_user_name: "martin"
git_user_email: "martin@nonya.com"

# Shell preferences
default_shell: "/bin/zsh"
install_oh_my_zsh: true

# Editor preferences
default_editor: "nvim"

# Python preferences
python_version: "3.11.9"
python_venv_name: "default"

# Backup existing dotfiles
backup_dotfiles: true